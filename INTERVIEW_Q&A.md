# 部门智能 RAG 问答助手 — 面试问答预演

> 基于简历描述 + 面试经验教训，准备面试中可能被追问的所有细节。

---

## 一、项目整体

**Q: 先介绍一下这个项目？**

A: 我做的是一个面向部门垂直领域的智能问答助手，解决的是部门内关于算力集群机房各类参数、操作说明等问题的咨询效率低下的问题。核心是基于 LangChain 框架构建了全链路 RAG 架构，从数据清洗、多路召回、Cross-Encoder 重排、动态 Top-K 调度，到查询改写、多租户隔离，实现了完整的端到端 pipeline。

**Q: 这个项目解决了什么痛点？**

A: 有三个核心痛点：

1. **专有名词检索难**：GPU 型号（A100/H800/V100）、网络协议（InfiniBand/NVLink）、运维术语这些专有名词，语义向量空间里可能距离很远，纯向量检索容易漏召回
2. **长文档语义割裂**：一份算力集群文档几千字，直接按固定长度切分会把条款编号打散，比如"第三条第一款"和"第三条第二款"被切到不同 chunk，检索时只能命中其中一个
3. **上下文噪音**：给 LLM 太多不相关的 chunks 会造成上下文污染，影响答案质量，Token 消耗也高

---

---

## 三、多路召回

**Q: 为什么要混合检索？只用向量检索不行吗？**

A: 不行。向量检索对专有名词、型号编号的精确匹配能力弱。例如查询"A100 和 H800 的区别"，向量检索可能召回"GPU 型号对比"相关内容，但精确命中"A100"和"H800"这两个词的能力不如 BM25。混合检索让两者互补：BM25 负责精确匹配专有名词，向量检索负责语义泛化。

**Q: BM25 适合什么场景？**

A: BM25 擅长精确匹配：型号编号（"A100"、"H800"）、专有术语（"InfiniBand"、"NVLink"、"Fair-Share"）、编号体系（"第三条第一款"）。这些词在语义空间中可能因为训练数据偏差而距离很远，但 BM25 能通过词项匹配精确命中。

**Q: BM25 原理是什么？**

A: BM25 是一种基于词项频率的信息检索模型，是 TF-IDF 的改良版本。核心公式：

```
Score(d,q) = Σ IDF(t) × (tf × (k1 + 1)) / (tf + k1 × (1 - b + b × |d|/avgdl))
```

其中 k1 控制词频饱和度（避免词频过高时分数无限增长，通常 k1=1.5），b 控制文档长度归一化（通常 b=0.75）。我们使用 rank_bm25 库实现，中文使用 jieba 分词，英文按子词切分（对"A100"这样的型号做子词拆分）。

**Q: alpha 参数怎么调的？**

A: alpha 控制 BM25 和向量检索的权重：`final = alpha × BM25 + (1-alpha) × Dense`。alpha=1.0 是纯 BM25，alpha=0.0 是纯向量检索。我们通过网格搜索（alpha ∈ {0.3, 0.4, 0.5, 0.6, 0.7}）在评测集上找最优值，发现最终落在 0.5 附近——说明这两种方法在这个场景下贡献大致均衡。

**Q: RRF 融合是什么？**

A: Reciprocal Rank Fusion（倒数排名融合）是一种无参数的排序融合方法。公式：`RRF(d) = Σ 1/(k + rank_i(d))`，其中 k 是平滑参数（通常取 60），rank_i 是方法 i 中文档 d 的排名。RRF 的优点是不需要调参，且对不同尺度的分数不敏感。我们把它作为备选融合方案。

**Q: 为什么召回 top-20 而不是 top-5？**

A: 第一轮召回要保证高召回率，宁可多召回一些让后面的重排模块来筛选。如果首轮召回就很少，重排后的效果上限也会受限。top-20 是precision和recall的平衡点。

---

## 四、Cross-Encoder 重排序

**Q: 什么是 Cross-Encoder？和 Bi-Encoder 的区别？**

A: Bi-Encoder 分别编码 query 和 document，得到两个独立向量，全程无交互。Cross-Encoder 把 (query, document) 拼接后一起编码，attention 机制可以看到 query 和 document 之间所有 token 级别的交互，精度更高，但速度更慢（无法预计算 document 向量）。应用场景：Bi-Encoder 做首轮粗召回（快速），Cross-Encoder 做精排（准确）。

**Q: 重排后取多少个？**

A: 重排后取 top-10。这是因为：1) Cross-Encoder 计算代价高（需要逐对编码）；2) top-10 之后分数梯度变化小，信息边际收益递减；3) 太多 chunks 会给 LLM 造成上下文噪音。

**Q: Cross-Encoder 用什么模型？**

A: 使用 Qwen 本地模型做 zero-shot pairwise 分类。给定 (query, chunk)，让模型输出一个 0-1 之间的相关性分数。训练成本为零，直接推理使用。

---

## 五、Dynamic Top-K 调度

**Q: 为什么要动态 top-K？固定 top-K 有什么问题？**

A: 固定 top-K 有两个问题：1) 查得准的时候给太多 chunks → LLM 上下文冗余，token 浪费；2) 查不准的时候给太少 chunks → 关键信息遗漏，召回率下降。动态 top-K 根据分数分布判断"这次检索质量怎么样"，质量高时少给，质量差时多给。

**Q: 算法原理是什么？**

A: 基于分数分布的两个信号：**(1) 标准差（std）**：分数分散说明有明显的相关/不相关之分，少给 chunks；分数集中说明区分度低，需要多给 chunks 扩大搜索面。**(2) 分数梯度（gap）**：top-1 和 top-2 分数差距大说明第1名很突出，少给；差距小说明多个候选都差不多，多给确保不遗漏。综合公式：

```
top_k = clamp(
    initial_k + β × (std_threshold / (std + ε)) + γ × (1 / gap),
    min_chunks=3, max_chunks=10
)
```

其中 β=2, γ=1 是经验参数。

**Q: 阈值怎么定的？**

A: 在评测集上做 grid search。std_threshold 在 {0.05, 0.10, 0.15, 0.20} 中选最优，最终选了 0.15。经过数据分析发现：当 std > 0.15 时，前几名和后面差距明显，应该减少 chunks；当 std < 0.15 时，分数接近，需要更多 chunks 来确保信息覆盖。实际统计：约 60% 的 query 最终给 5-7 个 chunks。

---

## 六、查询改写

**Q: 为什么要做查询改写？**

A: 用户提问往往是模糊的、口语化的，和文档中的表述有差异。例如用户问"这个集群能跑多大的模型"，文档里写的是"单节点最大支持 70B 参数模型的分布式训练"。查询改写通过同义词扩展、指代消解、问题分解来弥合这个语义鸿沟。

**Q: 指代消解怎么做？**

A: 用 LLM 识别当前 query 中指向上下文的部分（如"它"、"这个"），然后结合历史对话窗口，把指代还原为具体实体。例如：问"A100 的显存多大"，下一句问"它的功耗呢" → 改写为"查询内容：A100 的显存和功耗"。

**Q: 子查询分解怎么做？**

A: 把一个复杂问题拆成多个简单问题并行检索，再合并结果。例如"比较 V100 和 A100 在训练 Transformer 时的性能差异" → 拆成 ["V100 训练 Transformer 性能", "A100 训练 Transformer 性能", "V100 和 A100 对比"]，并行检索后取各路 top-3 的并集。

---

## 七、多租户隔离

**Q: 多租户隔离怎么实现？**

A: 三层防护：1) **BM25 过滤**：检索时注入 tenant_id，BM25 只返回当前租户的 chunks；2) **向量检索过滤**：FAISS 检索时传入 metadata filter，只搜索当前租户的向量子空间；3) **后置过滤**：结果返回后再次校验 tenant_id。实际面试可以强调：这是行级安全，query 时注入 tenant_id，存储层完全隔离。

**Q: 性能影响？**

A: metadata 过滤会增加一点检索延迟（约 5-10ms）。优化方案：按 tenant 构建独立的索引分片，但会增加存储成本。这是安全和性能的 trade-off，我们选择了后过滤方案作为默认配置。

---

## 八、评测体系

**Q: 怎么知道你的 RAG 有效？**

A: 我们构建了完整的评测体系。首先构建了 200 条评测集 query，覆盖事实型（40%）、推理型（25%）、比较型（20%）、定义型（10%）、操作型（5%）五种意图类型，以及 easy（30%）、medium（50%）、hard（20%）三种难度级别。

**Q: 评测集怎么构造的？**

A: 分层采样。先由领域专家手工编写 query，再由标注员标注 ground truth chunks。确保覆盖所有 10 篇文档、按意图和难度分层、租户分布均衡。**（关键：不能只说 100 条，要说分层分布和构造方式）**

**Q: 指标有哪些？**

A: Recall@K（衡量召回率）、MRR（衡量排名质量）、NDCG@K（综合考虑位置权重）、HitRate@K（简单粗暴的命中率）。

**Q: Baseline 怎么设计？**

A: 消融实验：逐个去掉组件，看每个模块的独立贡献。设计了 5 个 baseline：固定 top-5、仅向量检索、仅 BM25、无重排、混合检索但无动态调度。这样能定位系统瓶颈。**（面试要点：不能说 Recall=0.81 就完了，必须有 baseline 对比）**

**Q: Recall=0.81 怎么判断好不好？**

A: 和 baseline 对比。我的 full_pipeline Recall@5=0.81，比固定 top-5 (0.72) 高了 12.5%，比纯向量检索 (0.68) 高了 19%。在 hard 难度的 query 上提升更明显（0.54 vs 0.31）。95% 置信区间不重叠，说明提升是统计显著的。

---

## 九、Agent 与 Reflection

**Q: 完整的 RAG pipeline 是怎样的？**

A: query → 查询改写 → 多路召回 → Cross-Encoder 重排 → Dynamic Top-K → parent_chunk 回溯 → LLM 生成 → Reflection 校验 → 答案输出

**Q: Reflection 机制是什么？**

A: Reflection 是一个自检验循环：1) LLM 生成答案后，让它判断答案是否忠实于检索到的 context；2) 如果不忠实（幻觉/超出范围），触发新一轮检索；3) 最多重试 3 次。

**Q: 失败 3 次后怎么处理？**

A: 返回"I don't know"并附上最相关的检索片段，告诉用户"库里没有相关信息"。超过 3 次继续重试只会浪费 token 且结果不会变好。**（面试要点：不能只说 3 次，要解释为什么是这个数字）**

**Q: Agent 怎么决定调哪个工具？**

A: 我们的 RAG Agent 目前是固定 pipeline。如果做多工具 Agent，会用 ReAct 框架：Thought → Action → Observation → ... → Final Answer，其中 Action 由 LLM 决定调用哪个工具（search/calculator/document）。

---

## 十、系统设计

**Q: 如果文档量从 10 篇增长到 1000 篇，系统怎么扩展？**

A: 三个层面的扩展：1) **向量索引**：从 IndexFlatIP（精确搜索）切换到 IVF-PQ（倒排索引 + Product Quantization），支持亿级向量；2) **BM25 索引**：分片存储或使用 Elasticsearch；3) **多租户**：按 tenant 构建独立索引分片，查询时路由到对应分片。

**Q: 延迟是多少？**

A: 实测端到端延迟（不含 LLM 生成）：查询改写 ~200ms，多路召回 ~150ms，Cross-Encoder 重排 ~500ms（10 个 chunks），Dynamic 调度 ~10ms。LLM 生成根据 token 数量和模型大小变化。使用流式输出改善用户体验。

**Q: 最大的技术难点是什么？**

A: 是分块策略和评测体系。分块策略要平衡召回精度和语义完整性，不同类型的文档需要不同的分块策略。评测体系要保证评测集的质量和多样性，才能真正验证系统效果。

---

## 十一、业务理解（防崩要点）

**Q: 你觉得字节做 AI 应用最大的瓶颈是什么？**

A: 我觉得是数据质量和场景落地。LLM 能力已经足够强，但真正落地时，行业知识库的构建成本、数据鲜度维护、多租户安全隔离等工程问题，才是决定产品能否大规模使用的关键。**（不要瞎猜，结合面试公司的产品聊）**

**Q: 你了解扣子（Coze）吗？**

A: 扣子是字节的 AI Agent 平台，支持通过拖拽式工作流编排多工具调用，同时支持 RAG、知识库管理、插件扩展。它既是 Agent 平台（支持多轮对话、工具调用），也是工作流平台（支持条件分支、循环、并行执行）。它和 Dify、Coze Studio 等工具定位类似，但扣子在字节生态内有更深的集成。