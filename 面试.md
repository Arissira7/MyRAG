
RAG 流程：
   - 离线阶段：文档加载 切割（Chunking） 向量化（Embedding） 入库
   - 在线阶段：Query 优化 向量检索（粗排） Rerank（精排） 拼接 prompt  LLM 生成

## Chunking
核心设计思想：
- Child Chunk (256 token): 用于精准召回，小粒度匹配查询
- Parent Chunk (512 token): 用于完整上下文，提供语义完整的段落
- 表格作为原子单元：表格不被打散，整体作为一个 segment 参与分块
- 动态 chunk 大小：语义边界优先，实际大小在 400-800 token 范围内浮动

面试应答要点:
### Q: 总共多少文档？切了多少个 chunk？
A: 我构建了篇算力集群领域的模拟文档，覆盖 GPU 型号参数表、网络拓扑、机柜温控手册、调度策略、分布式训练指南、存储系统说明、故障排查手册、安全合规手册、能效评估、申请流程。总共切出了约 700 多个 chunk，其中 parent chunk 约 200 个，child chunk 约 500 个

### Q: 切片粒度怎么选？
A: 不是固定的。这是因为三个因素叠加的结果：
   1. 语义边界优先：我们在 512 token 限制内，优先保证在标题、空行、标点等语义边界处切分
   2. 表格原子性：表格不打散，整体作为一个 segment。如果表格本身超过 512 token（比如参数对照表），
      会强制按句号切分，但每个子块仍然保留完整表格结构
   3. 父子索引嵌套：parent chunk 先按语义边界聚合成 400-800 token 的块，再从中切出 150-500 token 的 child chunk
   这是设计权衡的结果——选择语义完整性 + 表格完整性，代价是放弃固定 chunk 大小。实际测试中，
   parent chunk 分布在 400-800 token，child chunk 分布在 150-500 token，对召回率影响在可接受范围内。

### Q: 父子索引解决了什么问题？
A: 长文档直接切片会打散语义。例如"第三条第一款"和"第三条第二款"
   如果被切到不同的 chunk，检索时只能命中最相关的那个。
   父子索引通过 child 定位 + parent 回溯，确保能拿到完整的语义段落。

### Q: 文档里有表格怎么处理？
A: 我们的策略：先识别 markdown 表格结构 → 序列化表格 → 作为独立 segment 参与
   分块。
   表格行数超过阈值时（>50行）才横向切分，否则整体保留。
   如果表格前后的文本段落也很长，表格会随段落一起移入下一个 parent chunk。

### Q: 怎么按语义边界切？
A: 按标题层级、表格边界、条款编号体系来识别语义边界。
   separator 优先级：[## 标题, 表格, # 大标题, ### 小节, 空行分段, 句号, 分号]
   表格优先级仅次于标题，因为它是最强的语义原子。

### Q: chunk 之间有重叠吗？
A: 有，overlap 设置为 64 token（约 2-3 个句子）。重叠的目的是保证边界信息不丢失。
   如果一个条款恰好跨在两个 chunk 的边界，重叠能保证关键信息不会被切断。

## 向量化（Embedding）
Embedding 算法大致经历了三代演进：
- 第一代是静态词向量，以Word2Vec 和 Glove为代表，把每个词映射成固定向量，但同一个词不管上下文是什么，向量永远不变，处理不了多义词。
- 第二代是以 BERT 为代表的上下文相关向量，同一个词在不同语境下有不同的向量，表达能力大幅提升，但 BERT 本身输出的是 token 级别的向量，两个句子要比较相似度就必须拼在一起跑，百万条文档就要跑百万次，检索速度完全不可接受。
- 第三代是以 SBERT、SimCSE、BGE 为代表的句子级对比学习 Embedding，专门为「两段文本有多相似」这个任务优化，能提前把所有文档向量算好存起来，查询时只需算一次，是 RAG场景的标配。



## 入库
用 Qdrant：
- 规模目前还没有很大，不需要用到 Milvus，而且 Milvus 运维很复杂。同时 Milvus 有的 Qdrant 基本都支持，比如 k8s部署，混合检索（Metadata 过滤），HNSW
- API简洁，Rust 写的性能很稳


### Q:讲讲你用的向量数据库？数据量级是多大？性能如何？遇到过性能瓶颈吗？
A:我们项目用的是 Qdrant，总共约 700 多个向量，包括 parent chunk（约 200 个，512维）和 child chunk（约 500 个，256维），
用 HNSW 索引，单次查询延迟在 10-30 毫秒。选 Qdrant 主要是因为它轻量易部署、API 简洁，Rust 实现性能很稳，
而且支持我们后续扩展到 k8s 部署和混合检索（Metadata 过滤）。

说实话数据量级还不大，暂时没遇到明显的性能瓶颈。不过 如果后续数据量涨到百万级，Qdrant 也完全 hold 住，它和 Milvus 一样支持 HNSW 和分布式扩展，只是运维成本低很多。

### Q:为什么时候不用图数据库
A:
   图数据库的代价不小：你需要用 LLM 做「实体抽取」和「关系抽取」来把非结构化文档转成图结构，这个过程成本高、容易出错，而且后续维护图结构比维护向量库复杂得多。

   如果用户的问题大多数是「找某个概念的解释」「某个功能怎么用」，向量检索加上 Rerank 已经够用了，强行上图数据库是过度设计。

   判断要不要用图数据库的简单原则： 问题里是否同时出现多个具体实体名，并且在问这些实体之间「有什么关系」或「通过关系能找到什么」 。如果是，就值得考虑图增强；如果问题主要是找某段描述性的内容，向量检索就够了。

## Query 优化
### HyDE
Q: 你是怎么做 Query 优化的？HyDE 是什么原理？
A: 
   1. HyDE（Hypothetical Document Embeddings）做 Query 优化。
      原理是这样的：

      传统的检索是直接用用户 Query 去向量库匹配，但用户 Query 通常很短、很口语化，和文档的表述方式差异很大，检索效果就不稳定。

      HyDE 的思路是「先猜测，再比对」——先用 LLM 根据用户 Query 生成一个「假设性文档」，这个文档用更正式、更接近文档风格的语言回答用户的问题，然后再用这个假设文档去向量库检索。

      举个例子，用户问「A100 和 H100 怎么选」，HyDE 会先生成一段类似「A100 和 H100 的选型指南：从算力、显存、功耗、成本等维度对比...」的假设文档，再用这个文档去检索，更容易命中真实的文档段落。

这个方案对短查询和口语化查询效果提升明显，不过每次查询都要多调一次 LLM，有额外的延迟和成本
### Query Decomposition
-  2. Query Decomposition
      触发条件：
         1. 复合意图（Composite Intent）
            • 问题中出现对比、列举、分别等逻辑关系
            • 多个实体同时出现
         2. 复杂语义/子问题独立性
            • 每个子问题可以单独进行检索
            • LLM 可以判断当前问题能否拆分
      实现上：
         可以纯LLM 判断，也可以结合规则（关键词＋实体数量）辅助，提高拆分可靠性
            • 检测关键词：对比、区别、列出、分别
            • 检测实体数量：>=2个实体触发拆分
            • 与 LLM 结果结合 —>提高准确率

## 向量检索（粗排）
### Q: 你们的 RAG 流程里，向量检索是怎么做的？为什么用混合检索？
A: 我们用了混合检索（Hybrid Search）的方案（向量检索 + BM25 关键词检索双路并行），用 RRF 算法合并结果。
   - 选择混合检索是因为两者各有盲区，正好互补：
      - 向量检索擅长语义理解，「GPU 算力不足怎么办」和「显卡性能瓶颈解决方案」这种不同表述能匹配上，但对「A100」「H100」这类精确型号词不敏感，容易漏。
      - BM25 关键词检索正好相反，对专有名词、型号、缩写精准命中，但无法处理同义词
      用户搜「显卡」，文档写的是「GPU」，BM25 直接返回零分。

   - RRF（Reciprocal Rank Fusion）的思路是只看排名不看分数，因为向量相似度和 BM25 的量纲不同，直接加权没意义。两路都高排的文档会排在最前面，只有一路命中的也不会被完全丢掉。
   这样做还有一个好处：两路检索是并行的，总延迟取最大值而不是相加，几乎没有额外时间代价。

### Q: 向量检索用什么索引？为什么选 HNSW ？
A: 我们用的是 Qdrant，它底层用的就是 HNSW（Hierarchical Navigable Small World）索引。
   - HNSW 本质上是一种近似最近邻（ANN）算法，在检索精度和速度之间做了很好的平衡。
它会构建一个多层的图结构，越高层越稀疏、越快，用来快速定位大致范围；
越底层越稠密、越准，用来精细搜索。
   - 相比 IVF、PQ 这类索引，HNSW 的优势是查询延迟低、精度高，适合对延迟敏感的业务场景。
缺点是内存占用相对高一些，但对我们几百个向量的规模完全不是问题。

### Q:为什么不加上 多query 进行三路召回
A:不是每个场景都需要三路全上，按业务特点来选：
   - 知识库里有大量专有名词、产品型号、数字的场景（比如电商、IT文档），向量＋BM25双路是标配，收益很明显。其实已经足够了
   - 用户提问方式多变、和文档表述差异大的场景（比如客服间答），加上多 Query扩展，才可能比较有效

## Rerank（精排）
使用 **Qwen3-Reranker-4B**
Rerank 的流程很清晰：多路召回得到 20~30 个候选 chunk，Cross-encoder Rerank 模型逐一给每个「(query, chunk)」对打相关度分，按分数降序排列后，取 top-3 到 top-5 拼入 prompt。


## RAG 系统中大模型的幻觉问题你怎么处理？

我把幻觉分为两类，来源不同，解法也不同。

- 第一类是「检索层幻觉」——检索没召回到相关内容，prompt 里是空的，LLM 用自己的知识瞎编。
- 第二类是「生成层幻觉」——检索召回了，但 LLM 没有严格照着说，在资料基础上加了自己的推断。

针对这两类，我用了三层防控：

- 第一层是 Prompt 强约束。在 system prompt 里明确立规矩：只能根据资料回答，资料没有就说不知道，不要推断和补充。LLM 有了「逃生出口」，就不会硬编。

- 第二层是检索质量门控。Rerank 模型会给每个 chunk 打 0-1 的相关度分，我会设一个阈值，如果最高分低于阈值，说明这次检索根本没找到有用的内容，直接返回「知识库无相关信息」，不让 LLM 在低质量上下文上硬撑。这个阈值要根据业务数据调，我一般从 0.4 开始，看误判样本往上或往下调。

- 第三层是生成后引用核查。LLM 生成完答案，再调用一次让它逐条检查：每条关键结论在 chunk 里有没有来源依据，没有就标注「无法核实」或者删掉。不过这个方案成本高，我只在对准确性要求极高的场景用。

总结一句话：治幻觉先治检索，检索做好了，Prompt 约束就能兜住大部分问题。

## RAG 评估体系
### Q: 你怎么知道你的 RAG 有效？用什么指标衡量？

A: 离线层分两层来看——检索层和生成层，指标不一样。

**检索层用 Hit@K 和 MRR**：
- Hit@5 是「正确 chunk 有没有在前5个结果里」，我目前是 0.81，说明 81% 的问题能召回到正确内容
- MRR 是「正确 chunk 排第几」，我目前 0.65 左右，说明召回的内容虽然找到了，但排名不够靠前

**生成层用 RAGAs 框架**：
- Faithfulness（忠实度）：答案里每句话有没有 chunk 依据，目标 >0.8
- Answer Relevancy（答案相关性）：答案有没有回答用户问题，目标 >0.8
- Context Recall（召回率）：检索结果覆盖了多少必要信息，目标 >0.7

需要注意的是，RAGAS 本质上是「LLM-as-a-Judge」，每次评估都要调用LLM 来打分。如果测试集有几千条，全量跑一遍的token 消耗和时间成本相当可观。工程上通常有两种缓解方式：一是对核心测试集抽样评估，只跑最有代表性的200~500条；二是把评判者模型从GPT-40 降级到 GPT-40-mini，成本降低10倍，精度损失在可接受范围内。

**线上指标**：
   - 踩率 （thumbs_down_rate）是最直接的信号，用户主动点踩，说明这次回答让他不满意，是最真实的负反馈。
   - 追问率（followup_rate）反映的是「答非所问」的程度，用户紧接着说「你没回答我的问题」或者追问同一个问题，通常意味着上一次回答没用。
   - 转人工率（escalation_rate）衡量的是「RAG放弃回答」的频率，这个比例太高说明知识库覆盖不足；但如果这个比例因为加了质量门控而上升，不一定是坏事，宁可转人工也不要给用户错误答案。
   - 空回答率（answer_empty_rate）就是系统主动说「我不知道」的比例，过高说明知识库亟需扩充。
   - 会话解决率（session_resolution_rate）是最综合的指标，衡量「一次对话能不能解决用户的问题」，是最贴近用户体验的衡量维度。
   
离线评估（Hit@K + RAGAS）用来快速迭代和定位问题，线上指标（踩率、转人工率）是最终验收标准。两者结合，形成「离线测评-＞上线->线上观测->发现问题->离线复现->修复->再上线」的完整评估闭环。


### Q: 评测集多少条？怎么构造的？

A: 我构造了约 100 条测试用例，分三层分布：
- 基础查询（40%）：单点问题，比如「A100 显存多大」
- 语义变异（30%）：同义表述，「显卡的内存」vs「GPU 显存」
- 组合问题（30%）：多实体对比、流程类问题

### Q: 你的 Recall 是 0.81，你怎么知道这个数字是好是坏？baseline 是什么？

A: 数字本身看不出好坏，一定要做对比。我每次加优化前会先测一版 baseline：

| 配置 | Hit@5 | MRR |
|------|-------|-----|
| 只有向量检索（baseline） | 0.72 | 0.50 |
| +BM25 混合检索 | 0.81 | 0.55 |
| +Rerank 精排 | 0.81 | 0.65 |

这样我可以说：加 BM25 让 Hit@5 提升了 9 个点（0.72→0.81），Rerank 对 MRR 提升更明显（0.50→0.65）。

没做 baseline 就只能回答"效果还行"，追问"和什么比还行"就答不上来了。

### Q: 那你的 0.81 够用吗？还要继续优化吗？

A: 够不够用要看业务场景。0.81 意味着每 5 次问答大概有 1 次召回失败，我评估了一下：
- 对精度要求高的场景（比如故障排查），这个比例偏高，需要优化到 0.9 以上
- 对召回要求宽松的场景（比如知识概览），当前够用

后续优化方向是加 Query 扩展或者多路召回，继续提升 Context Recall。


## 多租户隔离

### Q: 多租户隔离怎么实现？

A: 三层防护：1) BM25 过滤：检索时注入 tenant_id，BM25 只返回当前租户的 chunks；2) 向量检索过滤：FAISS 检索时传入 metadata filter，只搜索当前租户的向量子空间；3) 后置过滤：结果返回后再次校验 tenant_id。实际面试可以强调：这是行级安全，query 时注入 tenant_id，存储层完全隔离。

### Q: 性能影响？

A: metadata 过滤会增加一点检索延迟（约 5-10ms）。优化方案：按 tenant 构建独立的索引分片，但会增加存储成本。这是安全和性能的 trade-off，我们选择了后过滤方案作为默认配置。


